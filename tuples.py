# these are similar as list, but these
# are immutable, i.e they cannot be modified
# only two methods of list are available in tuples those are:
# they are index and count.
number=(1,2,3)
print(number[0])
# number[1]=3, wrong